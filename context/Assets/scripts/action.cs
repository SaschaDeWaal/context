using UnityEngine;
using System.Collections;

public class action : MonoBehaviour {

	/* This methode is called when the player does a action
	 * playerId is the number of the player, distance is the distance betwheen the action object and the player
	 */
	public virtual void Run(int playerID, float distance, GameObject obj){
		
	}
}
